game_surname:
  ru:
    regex: "^(?P<name>([А-ЯЁ][А-яЁё,0-9]{1,15}|[A-Z][A-z,0-9]{1,15}))$"
    min_length: 1
    max_length: 2
  en:
    regex: "^(?P<name>([A-Z][A-z,0-9]{1,15}))$"
    min_length: 1
    max_length: 2
server:
  ru:
    enum:
      - ""
